<?xml version="1.0" encoding="utf-8"?>
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
  <shortcut
    android:enabled="true"
    android:shortcutId="glowna-aktywnosc"
    android:shortcutShortLabel="@string/pierwszy_skrot_krotka_nazwa"
    android:shortcutLongLabel="@string/pierwszy_skrot_dluga_nazwa">

    <!-- Each one of these intents needs an android:action, even if you don't
         do anything with it. Otherwise you're in for a crash! -->
    <intent
      android:action="android.intent.action.VIEW"
      android:targetClass="org.qtproject.example.notification.IntercomAtivity"
      android:targetPackage="org.qtproject.example.notification">
      <extra android:name="key" android:value="value" />

    </intent>
    <categories android:name="android.shortcut.conversation" />

    <!-- If your shortcut is associated with multiple intents, include them
         here. The last intent in the list is what the user sees when they
         launch this shortcut. The previous intents will be put on the backstack -->
    <!-- You can also specify the category of the shortcut via the <categories>
         tag. As of this writing, the only supported category is
         android.shortcut.conversation, and can be specified using android:name
         like so: -->
    <!-- <categories android:name="android.shortcut.conversation"/> -->

  </shortcut>


    <shortcut
      android:enabled="true"
      android:shortcutId="komunikator"
      android:shortcutShortLabel="@string/drugi_skrot_krotka_nazwa"
      android:shortcutLongLabel="@string/drugi_skrot_dluga_nazwa">

      <!-- Each one of these intents needs an android:action, even if you don't
           do anything with it. Otherwise you're in for a crash! -->

        <intent
          android:action="android.intent.action.VIEW"
          android:targetClass="org.qtproject.example.notification.IntercomActivity"
          android:targetPackage="org.qtproject.example.notification">

        </intent>

      <intent
          android:action="android.intent.action.VIEW"
          android:targetClass="org.qtproject.example.notification.Shortcuts"
          android:targetPackage="org.qtproject.example.notification">
          <extra android:name="skrot" android:value="value" />

        </intent>
        <categories android:name="android.shortcut.conversation" />

    </shortcut>

</shortcuts>
